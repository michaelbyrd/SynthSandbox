<div class="row">
  <div class="sounds">
    <% if sound.title %>
      <%= link_to "#{sound.title}", sound %>
    <% else %>
      <%= link_to "Sound #{sound.id}", sound %>
    <% end %>
    <hr>
  </div>
</div>
